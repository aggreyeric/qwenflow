# XPRIZE Claim Verification — QwenFlow

> **Purpose:** Map **every** claim in `GEMINI_XPRIZE_SUBMISSION.md` to the specific `file:line` that proves or refutes it. XPRIZE judges are rigorous — this table is the artifact that defends the submission under scrutiny. Generated by reading `src/`, `tests/`, `public/`, and `package.json` directly.
>
> **Legend:** ✅ VERIFIED (code proves it) · ⚠️ OVERSTATED (real core, wording too strong) · 🔴 VAPORWARE (claimed, not built) · 🗺️ ROADMAP (architecture supports it, not yet wired)
>
> **Audit method:** `grep -rn` for every capability; ran `npm test` (103/103, 10 suites green); checked `package.json` deps; inspected UI handlers.

---

## Status legend & headline finding

**Headline:** The submission's *architecture and Gemini integration are real and tested.* Three categories of claim need correction before judging: (1) **OpenAI/Anthropic are named but not built** (only Qwen + Gemini exist); (2) **test counts are stale** (claims 78/8, reality 103/10 — understating, but inaccurate); (3) **a few feature words are stronger than the code** (drag-and-drop, branch/loop, "no `any`"). The new `XPRIZE_JUDGES_DEMO.md` is written to be 100% defensible; this table tells you exactly what to fix in `GEMINI_XPRIZE_SUBMISSION.md` if Eric wants the original doc updated.

---

## One-Liner & "Why it beats LangChain/CrewAI"

| # | Claim (verbatim) | Evidence (file:line) | Status | Note |
|---|---|---|---|---|
| 1 | "route workflows across **Qwen, Gemini**, and any LLM" | `src/models.ts:27` `callModel`; `:28` routes `startsWith("gemini-")`; `src/gemini.ts:12` | ✅ VERIFIED | Qwen + Gemini both fully wired & tested. |
| 2 | one-liner: "**78 passing tests**" | `npm test` → 103 passed, 10 suites | ⚠️ OVERSTATED (number wrong) | Reality is **103/10**. Understating, but fix to 103. |
| 3 | "True model-agnostic orchestration — routes any step to **any provider (Qwen, Gemini, OpenAI, Anthropic)** at runtime" | Registry has only Qwen + Gemini: `src/models.ts:4–11`; no `openai`/`@anthropic-ai` in `package.json`; grep for `openai\|anthropic` in `src/` → empty | 🔴 VAPORWARE | **OpenAI & Anthropic are not implemented.** Router is provider-agnostic (prefix dispatch) so they're a 1-file adapter away, but as written this is unproven. |
| 4 | "LangChain/CrewAI lock you into one model paradigm" | — (opinion / market positioning) | 🗺️ N/A | Subjective claim; not code-verifiable. Soften or cite. |

---

## Key Features (1–10)

| # | Claim | Evidence | Status | Note |
|---|---|---|---|---|
| 5 | "**Multi-LLM routing** — Qwen, Gemini, OpenAI, Anthropic in one workflow" | Qwen+Gemini: `src/models.ts:27`; OpenAI/Anthropic: not present (see #3) | 🔴 PARTIAL VAPORWARE | Same as #3. State "Qwen + Gemini today; OpenAI/Anthropic via the same adapter pattern." |
| 6 | "**Visual workflow editor** — drag-and-drop node graph in the browser" | UI exists & is interactive: `public/index.html` ("Run Workflow" btn, model badge, results panel). **No drag/drop event handlers** (`dragstart`/`drop`/`draggable` → none found) | ⚠️ OVERSTATED | Real visual editor, but it's click-to-build, **not drag-and-drop**. Drop "drag-and-drop". |
| 7 | "**REST API** — every engine capability exposed" | `src/routes/models.ts` (list/capabilities/chat), `src/routes/workflows.ts` (create/run) | ✅ VERIFIED | Express routers; covered by `tests/routes.test.ts`, `tests/api.test.ts`. |
| 8 | "Agent orchestration — chain, **branch, loop**, fan-out/fan-in" | Chain: `orchestrator.ts:63`; fan-out/parallel: `:84` (`Promise.all`); **branch/loop**: none — grep `condition\|loop\|branch` in `orchestrator.ts` → empty | ⚠️ OVERSTATED | Only **chain + parallel fan-out** are implemented. Branch/loop not built. |
| 9 | "**Model registry & selector** — swap providers per-step at runtime" | `src/models.ts:4` (MODEL_REGISTRY); per-step `model` field: `src/types.ts:7` | ✅ VERIFIED | Each `WorkflowStep.model` is independent; routing is per-step. |
| 10 | "**Live execution tracing** — token usage, latency, output per node" | Tokens+latency per call: `src/types.ts:38` (`ModelResponse.tokens/latency`); aggregated per-node into `WorkflowRun.results`: `orchestrator.ts:95` | ✅ VERIFIED (data) | Per-node data captured. A dedicated trace-viewer panel is partial — call it "per-node token/latency/output" (true) not "live tracing UI". |
| 11 | "**Pluggable adapters** — add a new LLM in ~1 file (see `src/gemini.ts`)" | `src/gemini.ts` is **46 lines** and is the complete Gemini integration; `callModel` dispatches by prefix | ✅ VERIFIED | Credible — `gemini.ts` is a real, complete reference adapter. |
| 12 | "**TypeScript end-to-end, no `any` in the hot path**" | `src/qwencloud.ts:83` → `const data = await response.json() as any;` | ⚠️ OVERSTATED | There **is one `as any`** in the API-response parse. Change to "minimal `any`, isolated to SDK-response parsing" or fix the cast. |
| 13 | "**Self-hostable** — runs locally or in Docker" | `Dockerfile`, `docker-compose.yml` both present | ✅ VERIFIED | |
| 14 | "**Cross-model demos** — Analyze with Qwen → Slide with Gemini → Format with Qwen" | Engine supports any per-step model mix: `src/models.ts:27`; demo flow reproducible at runtime | ✅ VERIFIED (capability) | Capability is real; a named demo artifact is optional. |

---

## Test Results table (the "78/78, 8 suites" block)

| # | Claim | Evidence | Status | Note |
|---|---|---|---|---|
| 15 | "78/78 passing, 8 suites" with a per-suite table (14+12+11+10+9+8+8+6=78) | `npm test` → **Test Files 10 passed (10) / Tests 103 passed (103)**; actual suites: `api, cli, gemini, orchestrator, qwencloud, routes, schemas, slack, store, types` | ⚠️ STALE/INACCURATE | Reality is **103 tests / 10 suites**. The old table omits `cli`, `gemini`, `slack`, `store` suites. Replace the whole table with the real 10-suite breakdown. |

---

## Tech Stack

| # | Claim | Evidence | Status |
|---|---|---|---|
| 16 | "LLM SDKs: `@google/generative-ai`, Qwen SDK, OpenAI-compatible clients" | `package.json` deps: `@google/generative-ai@^0.24.1` ✅; Qwen via DashScope `fetch` (`src/qwencloud.ts`) ✅; **no `openai` package** | ✅ VERIFIED (with caveat) | "OpenAI-compatible clients" = Qwen/DashScope speaks the OpenAI API shape. Fine as written; don't imply an `openai` SDK dep. |
| 17 | "Testing: Node test runner + custom harness" | Actually **Vitest** (`package.json`: `vitest@^2.0.0`, `"test": "vitest run"`) | ⚠️ INACCURATE | It's **Vitest**, not the Node test runner. Fix wording. |

---

## Architecture diagram

| # | Claim | Evidence | Status |
|---|---|---|---|
| 18 | Diagram box: "**OpenAI / Anthropic pluggable adapters**" | Not implemented (see #3) | 🔴 VAPORWARE | Remove or label "(roadmap)". |
| 19 | Diagram boxes: Qwen adapter (`src/qwen.ts`), Gemini (`src/gemini.ts`), Engine (`src/engine.ts`) | Real files: Qwen = `src/qwencloud.ts` (not `qwen.ts`), Gemini = `src/gemini.ts` ✅, Engine = `src/orchestrator.ts` (not `engine.ts`) | ⚠️ MISNAMED | Update filenames in the diagram to `qwencloud.ts` + `orchestrator.ts`. |

---

## Links

| # | Claim | Evidence | Status |
|---|---|---|---|
| 20 | "GitHub: https://github.com/aggreyeric/qwenflow" | Not verified from repo (no remote check performed per constraints) | ❔ UNVERIFIED | Confirm repo exists & is public before judging. |
| 21 | "UI screenshot: `docs/screenshot-ui.png`" | `docs/screenshot-ui.png` **exists** | ✅ VERIFIED |
| 22 | "Live demo (offline-capable): `docs/demo.html`" | `docs/demo.html` **does not exist** (dir has `gemini-demo.html`, `uipath-demo.html`) | 🔴 BROKEN LINK | Point to `docs/gemini-demo.html` (exists) or create `demo.html`. |

---

## Summary scorecard

| Category | Count |
|---|---|
| ✅ VERIFIED | 9 |
| ⚠️ OVERSTATED / INACCURATE / STALE / MISNAMED | 9 |
| 🔴 VAPORWARE / BROKEN | 4 |
| ❔ UNVERIFIED | 1 |
| 🗺️ N/A (opinion) | 1 |

**Highest-risk items to fix before Eric signs off (in priority order):**
1. **#3 / #5 / #18 — OpenAI & Anthropic.** This is the single biggest vaporware exposure. Either remove, or reframe as "Qwen + Gemini shipped; OpenAI/Anthropic use the same 1-file adapter pattern (see `gemini.ts`), on roadmap." The new `XPRIZE_JUDGES_DEMO.md` already does this reframe correctly.
2. **#15 / #2 — Test counts.** Replace 78/8 with **103/10** everywhere. (Re-running the 10-suite breakdown from `tests/`.)
3. **#6 — "drag-and-drop".** UI is a real editor but not drag-drop. Drop the phrase.
4. **#8 — "branch, loop".** Engine does chain + parallel fan-out only.
5. **#22 — `docs/demo.html`** broken link → swap to `docs/gemini-demo.html`.

**What is unimpeachably real (lean on this):** the Gemini adapter (`src/gemini.ts`, real `@google/generative-ai` calls), the per-step multi-model router (`src/models.ts:27`), the resilient DAG executor with retry + cross-provider fallback (`src/orchestrator.ts`), the typed capability registry including 2M-context Gemini 1.5 Pro (`src/models.ts:4`), the REST API (`src/routes/`), and **103/103 green tests including Gemini-SDK-contract tests** (`tests/gemini.test.ts`). That core is strong enough to anchor a $2M pitch.
